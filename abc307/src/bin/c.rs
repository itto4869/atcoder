use proconio::{fastout, input, marker::Chars};

#[fastout]
fn main() {
    input! {
        ha: usize,
        wa: usize,
        grida: [Chars; ha],
        hb: usize,
        wb: usize,
        gridb: [Chars; hb],
        hx: usize,
        wx: usize,
        gridx: [Chars; hx],
    }
    
}
