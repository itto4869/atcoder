use ac_library::LazySegtree;
use proconio::{fastout, input};

#[fastout]
fn main() {
    input! {
        n: usize,
        m: usize,
        mut l: [usize; n],
        mut r: [usize; m],
    }
    l.sort_unstable();
    r.sort_unstable();
    let mut cnts = vec![0; m];
    for li in l {

    }
}
